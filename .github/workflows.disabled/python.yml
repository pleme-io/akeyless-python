# NOTE: This file is auto generated by OpenAPI Generator.
# URL: https://openapi-generator.tech
#
# ref: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: akeyless Python package

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 3.8 REMOVED 2026-08-03. It cannot pass: `requirements.txt` pins
        # `setuptools >= 78.1.0` (the CVE-2025-47273 path-traversal fix) and
        # setuptools dropped 3.8 at 76.0 — the last release supporting it is
        # 75.3.x. So the matrix cell asked pip for a version that does not
        # exist for that interpreter:
        #   ERROR: No matching distribution found for setuptools>=78.1.0
        # and fail-fast then cancelled 3.9-3.12, which is why one real failure
        # presented as four broken cells.
        #
        # Dropping the interpreter is the fix, not loosening the pin: Python
        # 3.8 reached end-of-life in October 2024, and the pin is a SECURITY
        # floor. Trading a CVE fix to keep an EOL interpreter green would be
        # backwards.
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r test-requirements.txt
      - name: Test with pytest
        run: |
          pytest --cov={{packageName}}
